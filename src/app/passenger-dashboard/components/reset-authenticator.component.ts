import {Component} from '@angular/core';

@Component({
  selector: 'reset-authenticator',
  template:`
      <a routerLink="/two-factor">Nothing</a>
  `
})

export class ResetAuthenticatorComponent {}
